%header
  .ctrls
    %a{:href => '/upload'} Add a new image
  %h1
    Current Images
%ul
  - @images.each do |r|
    %li.uploaded_image
      %img{:src => r.file.url(:thumb)}
      .del
        %a{:href => "/pic/del/#{r.id}", :title => 'trash'}
          %img{:src => '/images/trash.png', :alt => 'trash', :title => 'trash'}
